# CodeQL configuration for open-computer-use.
#
# py/path-injection false positives are eliminated at the source:
# computer-use-server/security.py safe_path() uses os.path.realpath + startswith,
# which CodeQL natively recognises as a containment check barrier.
# No model extensions are needed.

name: "open-computer-use-codeql-config"
